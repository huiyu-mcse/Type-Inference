const nums = [1, 2, 3];
const doubled = nums.map(function(n) { return n * 2; });
const evens = nums.filter(function(n) { return n > 1; });
const joined = nums.join(",");
const hasTwo = nums.includes(2);
const len = nums.length;
