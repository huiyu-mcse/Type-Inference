var flag   = true;
var output = 0;
var delta  = 1;

function h(delta, output) {
  var flag = true;
  while (flag) {
    if (flag) {
      output = output + delta;
    } else {
      output = output + 0;
    }
  }
  var delta = "str";
}

